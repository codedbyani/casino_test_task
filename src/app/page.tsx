import { SideBar } from '@/components/molecules/SideBar';

const Home: React.FC = () => {
    return (
        <div className='w-full'>
            <SideBar />
        </div>
    );
};

export default Home;
